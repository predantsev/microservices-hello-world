spring:
  datasource:
    url: jdbc:mysql://mysql:3306/callcounter?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: example
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL8Dialect  # Optional: explicitly set the dialect
    show-sql: true